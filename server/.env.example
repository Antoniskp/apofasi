# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/apofasi

# Session configuration
SESSION_SECRET=replace-me
SESSION_NAME=apofasi.sid
SESSION_MAX_AGE_DAYS=7
# SESSION_SECURE - Optional: Override session cookie secure flag (true/false)
#   If not set, defaults to true in production (assumes HTTPS), false in development
# SESSION_SAMESITE - Optional: Override session cookie SameSite attribute (none/lax/strict)
#   If not set, defaults to 'none' in production (with secure=true), 'lax' in development
#   IMPORTANT: SameSite=None requires Secure=true (HTTPS). For HTTP deployments, use lax or strict.
# SESSION_SECURE=false
# SESSION_SAMESITE=lax
CLIENT_ORIGIN=http://localhost:5173
PORT=5000

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback

# Facebook OAuth
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret
FACEBOOK_CALLBACK_URL=http://localhost:5000/auth/facebook/callback

# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=http://localhost:5000/auth/github/callback

# Set to production when deploying behind HTTPS
NODE_ENV=development
